# Changelog

## 0.1.0

First public release.

- Levelled logging: `e`, `w`, `i`, `v`, `d`
- Optional log files with size-based rotation, one file per day
- Conditional IO/web layer, so the same code compiles for Flutter web
- Repeat suppression through `repetitionCount`
